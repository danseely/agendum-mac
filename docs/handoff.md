# Handoff

> Historical file. Do not use this file for current operational instructions.

Current project state lives in `docs/project-state.md`. Objective feature status lives in `docs/features.json`.

This split handoff file previously carried active branch, checkpoint, validation, and next-action guidance. That body was retired on 2026-05-07 because it had become stale after A5 work opened PR #38 from `codex/a5-module-rename` to `feature/mac-prototype`.

As of 2026-05-08, PR #43 has merged into `feature/mac-prototype` as `d516b86`, issue #42 is complete, and this file remains historical only.

For current work:
- Read `docs/project-state.md`.
- Read `docs/features.json`.
- Treat `feature/mac-prototype` plus the canonical state files as the current handoff source.
- Do not merge PRs unless the user explicitly asks.

Use git history for archaeology only; do not infer current branches, PRs, validation status, or next actions from old revisions of this file.
